services:

  nginx: # SERVICE NAME
    # user: 0:0 by default as nginx needs to start as root first but then creates its workers as nginx user
    build: ./nginx
    image: nginx:inception # IMAGE NAME
    pull_policy: build
    container_name: nginx # CONTAINER NAME
    environment:
      LOGIN: ${LOGIN}
    ports:
      - "${NGINX_BIND_ADDR}:443:443"
    volumes:
      - wordpress:/var/www/html
      - ./static:/var/www/static
    depends_on:
      - wordpress
    networks:
      - inception
    restart: always

  wordpress:
    build:
      context: ./wordpress
    image: wordpress:inception
    container_name: wordpress
    env_file:
      - .env
    environment:
      WORDPRESS_DB_NAME: ${MARIADB_DATABASE}
      WORDPRESS_DB_USER: ${MARIADB_USER}
    secrets:
      - mariadb_password
      - wordpress_admin_password
      - wp_user_password
    volumes:
      - wordpress:/var/www/html
    depends_on:
      - mariadb
    networks:
      - inception
    restart: always

  mariadb:
    build: ./mariadb
    image: mariadb:inception
    container_name: mariadb
    secrets:
      - mariadb_root_password
      - mariadb_password
    env_file:
      - .env
    volumes:
      - db_data:/var/lib/mysql
    networks:
      - inception
    restart: always

  redis:
    build: ./redis
    image: redis:inception
    container_name: redis
    networks:
      - inception
    restart: always

  ftp:
    # user: 0:0 by default as ftp needs to start as root first for [ bind, chpasswd, chown, PAM ]
    build:
      context: ./ftp
      # still run ftp service as root but client should run as www-data / match WP ( Alpine 82 )
      args:
        FTP_USER: ${FTP_USER}
    image: ftp:inception
    container_name: ftp
    ports:
      - "${FTP_BIND_ADDR}:${FTP_PORT}:21"
      - "${FTP_BIND_ADDR}:${FTP_PASV_PORT_RANGE}:21100-21110"
    secrets:
      - ftp_password
    environment:
      FTP_USER: ${FTP_USER}
    volumes:
      - wordpress:/var/www/html
    networks:
      - inception
    depends_on:
      - wordpress
    restart: always


  adminer:
    build: ./adminer
    image: adminer:inception
    container_name: adminer
    ports:
      - "${ADMINER_BIND_IP}:${ADMINER_PORT}:8080"
    env_file:
      - .env
    networks:
      - inception
    depends_on:
      - mariadb
    restart: always


volumes:
  wordpress:
    name: wordpress
    driver: local # enables local storage through docker's built in local driver
    driver_opts:
      type: none # don't use a separate filesystem
      o: bind # we use bind as we want to use a different location for the volumes
      device: /home/${LOGIN}/data/wordpress
  db_data:
    name: db_data
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /home/${LOGIN}/data/mariadb


networks:
  inception:

secrets:
  mariadb_password:
    file: ./secrets/mariadb_password.txt
  mariadb_root_password:
    file: ./secrets/mariadb_root_password.txt
  wordpress_admin_password:
    file: ./secrets/wordpress_admin_password.txt
  wp_user_password:
    file: ./secrets/wp_user_password.txt
  ftp_password:
    file: ./secrets/ftp_password.txt

